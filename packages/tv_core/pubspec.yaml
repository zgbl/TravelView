name: tv_core
description: TravelView 开放照片库核心引擎（纯 Dart，不依赖 Flutter）
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  crypto: ^3.0.3
  convert: ^3.1.1
  path: ^1.9.0

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
