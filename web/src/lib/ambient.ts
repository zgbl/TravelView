/**
 * 播放时的配乐 —— **接口留着，实现是空的。**
 *
 * 曾经在这里用 Web Audio 现场合成过一段持续音。省了版权和体积，
 * 但结果不像音乐，像电器嗡鸣，配在旅行回顾上只有反效果，所以拿掉了。
 *
 * 真要做配乐，正确的路是买一份商用授权的曲库（Artlist / Epidemic
 * 这类，一年一两百美元），挑几首不同气质的曲子作为静态资源，
 * 让用户在发布时选一首。合成音乐这条路不用再试了。
 *
 * 这个类保留成空壳，是为了**播放器里的调用点不用来回改**:
 * 等真的接上曲子，只要把这几个方法填上，加上 PLAYER_MUSIC = true 即可。
 */
export class Ambient {
  get running() { return false; }
  async start() { /* 还没有配乐 */ }
  async stop() { /* 还没有配乐 */ }
  /// 换站时的重音。有曲子之后可以在这里做 ducking 或加一记打点
  transit() { /* 还没有配乐 */ }
}

/**
 * 配乐功能的总开关。**关着** —— 关着的时候播放器里连按钮都不出现，
 * 因为一个点了没反应的按钮比没有按钮更糟。
 */
export const PLAYER_MUSIC = false;
